terraform {
  required_providers {
    hcloud = {
        source  = "hetznercloud/hcloud"
        version = "1.39.0"
    }
    ansible = {
        source  = "ansible/ansible"
        version = "1.1.0"
    }
  }
}

provider "hcloud" {
    token = "{{ target_provider.vars.token }}"
}