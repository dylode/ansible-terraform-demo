terraform {
  required_providers {
    linode = {
      source  = "linode/linode"
      version = "2.3.0"
    }
    ansible = {
      source  = "ansible/ansible"
      version = "1.1.0"
    }
  }
}

provider "linode" {
  token = "{{ target_provider.vars.token }}"
}